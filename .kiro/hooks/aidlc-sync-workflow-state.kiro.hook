{"version":"1.0.0","enabled":true,"name":"aidlc-sync-workflow-state","description":"Keeps the workflow's Current Stage in aidlc-state.md in step with what has actually happened, so the status line and progress display stay accurate. DELIBERATELY wired to 'shell', not 'spec': the IDE never fires a spec toolType, so spec would never run (do not 'optimize' this back to spec). Cost: the audit tail is read on every execute_bash; the hook self-gates idempotently so re-reads are correctness-safe, and the read is bounded by audit size.","when":{"type":"postToolUse","toolTypes":["shell"]},"then":{"type":"runCommand","command":"bun .kiro/hooks/aidlc-kiro-adapter.ts sync-workflow-state"}}
