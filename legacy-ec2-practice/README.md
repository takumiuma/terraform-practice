# legacy-ec2-practice

Terraform 学習時に書いた EC2 / Auto Scaling Group / ALB の練習用コード（リージョン `us-east-2`）。
本プロジェクトの本命である ECS 構成（`../infra/`）とは無関係で、参照用に退避したもの。

使う場合はこのディレクトリ内で `terraform init && terraform apply`。不要なら削除して構わない。
